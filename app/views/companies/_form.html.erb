<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="internship-data">
  <div class="position-3">
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :area_of_interest %><br>
    <%= f.select(:area_of_interest, Company.options_for_filtered_area) %>
  </div>
  
  <div class="field">
    <%= f.label :status %><br>
     <%= f.select(:status, Company.options_for_status) %>
  </div></div>
    <div class="position-3">
  <div class="field">
    <%= f.label :website %><br>
    <%= f.text_area :website %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
       <%= f.select(:city, Company.options_for_cities) %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div></div>
  <div class="field">
    <%= f.label :adress %><br>
    <%= f.text_area :adress %>
  </div></div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.trix_editor :description %>
  </div>
  <div class="actions">
        <button type="button" class="btn btn-default btn-lg">
  <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> <%= f.submit %>
</button>
  </div>
<% end %>
